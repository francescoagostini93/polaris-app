name: polaris
description: Polaris - Cognitive stimulation exercises app with Gemini AI and voice interaction.
publish_to: 'none'
version: 0.1.2+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  google_generative_ai: ^0.4.0
  speech_to_text: ^7.0.0
  flutter_tts: ^4.0.2
  provider: ^6.1.1
  shared_preferences: ^2.2.2
  flutter_email_sender: ^6.0.3
  intl: ^0.19.0
  flutter_markdown: ^0.6.18
  wakelock_plus: ^1.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
